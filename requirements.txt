# Core
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pydantic>=2.0
pyyaml>=6.0
joblib>=1.3
rich>=13.0
typer>=0.9

# Optional (graceful fallback inside the code)
# torch          # only needed for full neural world model (not required for EmpiricalBaseline)
# scikit-learn
# scipy
# matplotlib
# fastapi
# uvicorn
# faiss-cpu
# networkx

# optional: sympy (identity checks), scikit-learn, faiss-cpu, fastapi, uvicorn
