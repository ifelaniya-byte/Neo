# Redacted control-plane snapshot — `council-20260813-r001`

**Capture date:** 2026-08-13 (local workspace date)  
**Source tree:** `/home/user/old_session_files`  
**Git source commit:** unavailable — the recovered source tree contains no `.git` directory.  
**Release kind:** source-only handoff; generated locally from the recovered tree. No download, extraction, network request, or executable payload was run by the packager.

## Archive identity

| Field | Value |
|---|---:|
| ZIP path | `/home/user/council-20260813-r001.zip` |
| ZIP bytes (container) | `1,003,422` |
| ZIP size | `0.96 MiB` |
| Members | `91` |
| Uncompressed member payload | `12,852,997` bytes (`12.26 MiB`) |
| Sum of member compressed streams | recorded per file in companion manifest |
| SHA-256 | `7b6d65b4b6ba482135ba1c1681031a0cb405c3241cfb9e6bb94c01cc6dd1640e` |
| ZIP CRC test | `PASS` |
| Full per-member compressed sizes and SHA-256s | `council-20260813-r001.zip.manifest.json` |

The archive excludes VCS/cache directories, `.env`-style files, credential filenames, symlinks, and common model-tensor extensions (`.safetensors`, `.bin`, `.pt`, `.pth`, `.ckpt`, `.onnx`, `.gguf`). It contains source, tests, documentation, datasets, and small model-card/config metadata. It **does not** contain a model tensor.

## Current actual workspace size

| Item | Bytes |
|---|---:|
| Recovered control-plane source tree (including added packer) | `12,921,400` |
| Release ZIP | `1,003,422` |
| External archive manifest | `19,226` |
| Handoff run log | `445` |
| Unit-test log | `6,292` |

The recovered tree is approximately `12.32 MiB`, not the historical `~1.01 GB` claimed by its stale `program_census.json`. That older census names a 988,097,824-byte Qwen tensor, but the recovered tree contains only offline model cards/configuration files; the tensor is absent. This directly explains the missing-weight test failures.

## Evidence and runtime state

| Evidence | Observed status |
|---|---|
| Dataset `smoke-v2` | 60 records; SHA-256 `d880d026bbc799847774506043cfb44c6c0ce4846384393412e6f6e273d34bee` |
| Qwen manifest | Declares expected `model.safetensors` SHA-256 `fdf756fa7fcbe7404d5c60e26bff1a0c8b8aa1f72ced49e7dd0210fe288fb7fe`; actual tensor **absent** |
| Model config/tokenizer load | **unproven** |
| GPU access in this workspace | unavailable / not exercised |
| Worker result | no `worker_result.json` found |
| Predictions / evaluation / run report | none found |
| First Kaggle attempt | no evidence recovered; cannot claim it occurred |
| HF provider evidence | `REAL_INFER.json` records 0/8 completions and HTTP 402 responses (historical artifact) |

## Unit-test result

Command run:

```text
python3 -m unittest discover -s tests -v
```

**Result: NOT CLEAN.** 18 tests ran: **14 passed, 2 failed, 2 errored** (exit code 1).

| Test | Result | Cause |
|---|---|---|
| `test_qwen_weight_pin` | FAIL | Expected Qwen artifact is marked `exists: false`. |
| `test_artifact_gate_cli_ok_on_local_files` | FAIL | Artifact-gate CLI exits 1 because no complete model directory is present. |
| `test_find_complete_model_dir` | ERROR | No complete model directory found under `weights_offline/modelscope`. |
| `test_tokenizer_offline_safe` | ERROR | Expected `tokenizer_offline_safe` key is absent from the corresponding record. |

The remaining 14 tests passed, including secret-fragment, notebook-output, registry, ledger, smoke-v2 schema/hash, scorer-validation, and CPU-side worker-contract checks. Python emitted `ResourceWarning`s for unclosed test file handles; these warnings did not cause failures.

## Current blockers

1. Required Qwen weights are not in the recovered workspace; only manifests/cards/configs remain.
2. No Kaggle GPU attempt artifacts are available, so GPU, VRAM, model loading, predictions, evaluation, and second-clean-run claims remain unverified.
3. The recovered tree has no Git metadata, so no canonical source commit can be reported.
4. `program_census.json` is stale relative to this recovered tree and should be regenerated only after the actual artifact location is supplied.

## Redaction posture

No token values, cookies, private keys, authorization headers, signed URLs, or `.env` contents are included in this report, archive manifest, or release ZIP. The existing secret-fragment test passed, but that is a narrow content check rather than a complete security audit.

## Files produced

- `council-20260813-r001.zip` — source-only release bundle
- `council-20260813-r001.zip.manifest.json` — exact per-member uncompressed/compressed byte counts and SHA-256 digests
- `council-20260813-r001.handoff.log` — packer output log
- `council-20260813-r001.tests.log` — complete unit-test output
- `council-20260813-r001.REDACTED_SNAPSHOT.md` — this report
