# Redacted full snapshot — `council-20260813-r002`

**Status:** recovered control plane; source-only release.  
**Previous release:** `council-20260813-r001` (preserved unchanged).  
**Canonical Git commit:** `72dd6a517f97e836f8beea74336b65079d0aa9ea`  
**Git state at capture:** clean; `git fsck --no-reflogs` completed without findings.

## Changes since r001

1. Initialized a local Git repository and made two local commits.
2. Reworked `tools/make_handoff.py` into manifest schema v2:
   - hashes ZIP members by reading the completed ZIP, eliminating the source-tree TOCTOU issue;
   - records release ancestry and canonical commit;
   - avoids absolute paths in the public handoff log/manifest;
   - excludes VCS/cache directories, environment files, credential filenames, private-key suffixes, symlinks, and common weight-tensor extensions.
3. Split tests into `tests/unit/` and `tests/integration/`.
4. Made artifact-gate absence results have a stable schema.
5. Removed observed `ResourceWarning`s in exercised source and test paths.
6. Archived the stale pre-recovery census under `archive/census/` and regenerated the canonical census with **zero local weight tensors**.
7. Added `.gitignore` to keep credentials, caches, model tensors, generated ZIPs, and runtime logs out of Git.

## r002 release identity

| Field | Value |
|---|---|
| Release ID | `council-20260813-r002` |
| Previous release | `council-20260813-r001` |
| Release kind | `source-only` |
| Weights included | `false` |
| ZIP | `council-20260813-r002.zip` |
| Container bytes | `1,008,414` |
| Member count | `96` |
| Uncompressed member payload | `12,864,198` bytes |
| Sum of ZIP member compressed streams | `995,766` bytes |
| ZIP SHA-256 | `a7d103e8583f07b1ab44219037761f9ee3d651c15046f9d837a8c7b3c002f2a0` |
| ZIP CRC verification | `PASS` |
| Deterministic second pack | `PASS` — identical SHA-256 |

### Packer output log

```text
HANDOFF_RELEASE council-20260813-r002
SOURCE_ROOT [REDACTED_PROJECT_ROOT]
POLICY source-only; excludes VCS/caches/env files/credential filenames/private-key suffixes/symlinks/weight extensions
INPUT_FILES 96
ARCHIVE council-20260813-r002.zip
ARCHIVE_BYTES 1008414
ARCHIVE_SHA256 a7d103e8583f07b1ab44219037761f9ee3d651c15046f9d837a8c7b3c002f2a0
PAYLOAD_UNCOMPRESSED_BYTES 12864198
MEMBER_COUNT 96
CRC_CHECK PASS
MANIFEST council-20260813-r002.zip.manifest.json
```

The full redacted member-level manifest—paths, uncompressed bytes, compressed bytes, and SHA-256 digest for every ZIP member—is in `council-20260813-r002.zip.manifest.json`.

## Census and total size

The regenerated canonical census reports:

| Metric | Value |
|---|---:|
| Source payload files | 94 |
| Entire recovered program + retained r001/r002 release evidence | 16,547,673 bytes at capture (see `council-20260813-r002.sizes.log`) |
| Source payload bytes (excludes `.git` and caches) | 12,855,748 |
| Source payload physical bytes | 12,898,304 |
| Source operational footprint (includes local Git metadata/cache scope) | 12,901,400 |
| Actual local model tensors | 0 |
| Runtime-verified models | 0 |
| Integrity errors | 0 |

The expected external Qwen artifact remains **absent**. Its recorded expected `model.safetensors` size is `988,097,824` bytes and its expected SHA-256 is `fdf756fa7fcbe7404d5c60e26bff1a0c8b8aa1f72ced49e7dd0210fe288fb7fe`. This is a reference pin, not evidence that the tensor is present or verified.

`program_census.json` is the current canonical census. The historical 1 GB census was retained only as an archived, stale record under `archive/census/`.

## Test evidence

### Unit suite

Command:

```bash
python3 -W error::ResourceWarning -m unittest discover -s tests/unit -v
```

**PASS: 14 / 14.** Exit code 0. No `ResourceWarning` occurred under warnings-as-errors mode.

### Artifact integration suite

Command:

```bash
python3 -W error::ResourceWarning -m unittest discover -s tests/integration -v
```

**Result: 1 passed, 2 skipped.** The skipped tests require `COUNCIL_MODEL_DIR`; they are correctly reported as skipped because no external model artifact was supplied. They are not treated as passing model-validation evidence.

The complete stdout/stderr-safe logs are `council-20260813-r002.unit-tests.log` and `council-20260813-r002.integration-tests.log`.

## Security and provenance checks

| Check | Result |
|---|---|
| Custom regex secret scan | 77 scoped text/code files; 0 matches |
| Existing narrow token-fragment unit test | PASS |
| Gitleaks | Not installed in this environment; not claimed as run |
| Git ignored categories | credentials, caches, common model tensors/adapters, generated ZIPs/logs |
| Archive member paths | source-derived, relative paths only; symlinks excluded |

The custom scan is a limited heuristic, not a replacement for an audited secret scanner or human review before any remote push.

## Runtime / Kaggle evidence

| Evidence item | State |
|---|---|
| Current external model-artifact location | Not provided / absent from this workspace |
| Full external artifact manifest | Not available; expected-file pin only |
| Kaggle Dataset version | None created or supplied here |
| GPU attempt | None evidenced |
| `worker_result.json` | Absent |
| `predictions.jsonl` | Absent |
| `evaluation.json` | Absent |
| `run_report.json` | Absent |
| FP16 or NF4 metrics | Absent |

## Current blockers

1. Restore a license-compatible, official Qwen artifact outside Git and the source ZIP.
2. Verify the complete artifact and its trusted tensor digest in a staging location.
3. Supply `COUNCIL_MODEL_DIR` for artifact integration testing.
4. Perform a Kaggle GPU run and return its evidence bundle before making runtime, quality, VRAM, or reproducibility claims.

## Redaction

This snapshot and the associated source-only release omit tokens, cookies, authorization headers, `.env` contents, credential files, private keys, and signed URLs. Absolute project paths are omitted from r002 release metadata.
