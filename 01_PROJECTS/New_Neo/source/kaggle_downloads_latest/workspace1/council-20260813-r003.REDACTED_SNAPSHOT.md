# Redacted r003 snapshot

## Release

- Release: `council-20260813-r003`
- Previous release: `council-20260813-r002`
- Source commit represented by external manifest: `88a14d550259f74ff0a5430164267b22e3980fb5`
- Archive: `council-20260813-r003.zip`
- Archive bytes: `1,013,372`
- Archive SHA-256: `0308abb32a0ade2a99a9433abd755ba758b9011f99dcfcd0f706e2eb4835d042`
- CRC: PASS; member count: 101; uncompressed member payload: `12,874,337` bytes.
- Second deterministic pack: PASS (same archive SHA-256).

## r003 changes

- Repaired `CURRENT_STATE.json` release ancestry and runtime/artifact truth fields.
- Added `HANDOFF.md` and release-consistency unit tests.
- Moved the absent-artifact schema check into the unit suite; artifact integration now contains only two external-artifact tests.
- Added `tools/ingest_results.py`, a staged-only verifier for returned predictions and run reports. It validates local files, report schema, safe run ID, all benchmark IDs, and records hashes. It does not execute imported code or promote state automatically.
- Added `tools/size_report.py` for non-overlapping source/Git/release-evidence categories.

## Gates

| Gate | Result |
|---|---|
| Unit suite | PASS — 18/18; ResourceWarnings treated as errors |
| Artifact integration | BLOCKED — 2/2 skipped because `COUNCIL_MODEL_DIR` is absent |
| State-consistency tests | PASS — 3 tests included in unit total |
| Git status | clean |
| Git object check | `git fsck --no-reflogs` clean |
| Scoped custom secret scan | 82 eligible text/code files; 0 matches |
| Gitleaks | not installed; not claimed as run |
| Actual model tensors | 0 |
| GPU / worker / predictions / evaluation / run report | absent |

## Non-overlapping size report

See `council-20260813-r003.size-report.json` for the current generated totals. It separates source payload, Git metadata, external release evidence, and external model artifacts; no model artifact bytes are counted as present.

## Current blockers

1. External Qwen artifact is absent and no immutable official revision is recorded.
2. Artifact integration cannot execute without `COUNCIL_MODEL_DIR`.
3. No Kaggle GPU attempt or returned runtime evidence exists.

No API tokens, cookies, private keys, authorization headers, credential contents, `.env` contents, or signed URLs are included.
